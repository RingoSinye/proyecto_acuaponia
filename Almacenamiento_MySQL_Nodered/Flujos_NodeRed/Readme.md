Los documentos en formato JSON se deben cargar con la función import de Nodered.

Los documentos ExtraerDatosJSON y CambiarTimestamp se agregan como adicionales para poder revisarse y modificarse, basta con cargar el flujo principal
